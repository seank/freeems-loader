Welcome to FreeEMS-Loader!

To build this application please run the following command:

apt-get update;apt-get install libboost-dev libboost-system-dev

Then simply type "make" and hit enter!

It's as easy as that!

